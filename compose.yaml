services:
  frontend-dev:
    build:
      context: .
      target: development
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules

  frontend:
    build:
      context: .
      target: production
    ports:
      - "8080:80"
